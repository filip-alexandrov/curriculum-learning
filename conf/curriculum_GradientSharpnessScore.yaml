defaults:
  - _aucurriculum_score_
  - _self_

results_dir: /nas/student/FilipAlexandrov/results
experiment_id: default
device: cuda

hydra:
  sweeper:
    params:
      curriculum/scoring: GradientSharpnessScore_1
correlation:
  correlation_matrix: all
