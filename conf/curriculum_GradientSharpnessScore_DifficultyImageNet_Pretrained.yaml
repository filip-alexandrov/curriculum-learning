defaults:
  - _aucurriculum_score_
  - _self_

results_dir: /nas/student/FilipAlexandrov/results
experiment_id: SAM_DifficultyImageNet_Pretrained
device: cuda

hydra:
  sweeper:
    params:
      curriculum/scoring: GradientSharpnessScore_ResNet50T_lr0001
correlation:
  correlation_matrix: all
