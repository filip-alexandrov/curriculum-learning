defaults:
  - _aucurriculum_score_
  - _self_

results_dir: /nas/student/FilipAlexandrov/results
experiment_id: SAM_DifficultyImageNet
device: cuda

hydra:
  sweeper:
    params:
      curriculum/scoring: ClassificationTime,SharpnessScore_DifficultyImageNet
correlation:
  correlation_matrix: all
